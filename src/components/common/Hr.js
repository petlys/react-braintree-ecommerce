import styled from 'styled-components'
import { colors, spacing } from '../../styles'

export const Hr = styled.div`
  height: 1px;
  width: 100%;
  background-color: ${colors.TERTIARY_TEXT};
  margin: ${spacing.md} auto;
`

export default Hr
